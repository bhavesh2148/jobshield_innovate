# ============================================================
# Fake Job Detection System — Python Dependencies
# ============================================================

# Core ML
torch>=2.0.0
transformers>=4.35.0
sentence-transformers>=2.2.2
tokenizers>=0.14.0

# Classical ML
scikit-learn>=1.3.0
xgboost>=2.0.0
imbalanced-learn>=0.11.0

# Explainability
shap>=0.43.0

# Vector Store
faiss-cpu>=1.7.4

# Drift Detection
river>=0.19.0

# Data Processing
pandas>=2.0.0
numpy>=1.24.0
regex>=2023.8.8
tqdm>=4.65.0

# API Backend
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.4.0
python-multipart>=0.0.6
python-dotenv>=1.0.0          # Loads .env secrets at startup

# Serialization & Storage
# (joblib used internally by scikit-learn/xgboost — no direct import needed)

# Monitoring & Logging
loguru>=0.7.0

# Phase 6: OCR Ingestion (Tesseract local, offline)
pytesseract>=0.3.13              # Python bindings for Tesseract OCR binary
Pillow>=10.0.0                   # Image preprocessing (grayscale, sharpening)

# Development (install separately: pip install black pytest)
pytest>=7.4.0
